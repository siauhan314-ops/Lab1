{
  "name": "sweet-scoops-nuxt",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "if exist dist rmdir /s /q dist && mkdir dist && copy index.html dist\\ && copy styles.css dist\\ && copy script.js dist\\",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "nuxt": "3.10.3",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.5.1"
  }
}